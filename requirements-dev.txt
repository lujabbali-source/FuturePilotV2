# Dependencias solo para desarrollo y tests. Las de ejecucion estan en
# backend/requirements.txt.
#
#   pip install -r backend/requirements.txt -r requirements-dev.txt
#   python -m pytest
#
# httpx es lo que usa fastapi.testclient.TestClient por debajo: sin el, los
# 38 tests de tests/ no arrancan aunque pytest este instalado.
-r backend/requirements.txt

pytest==8.4.2
httpx==0.28.1
