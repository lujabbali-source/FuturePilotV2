import universities from "../universities/armenia";
import { defineCity } from "../citySchema";

export default defineCity({ id: "armenia", name: "Armenia", coordinates: { lat: 4.5339, lng: -75.6811 }, universities });
