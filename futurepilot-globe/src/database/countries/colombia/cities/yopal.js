import universities from "../universities/yopal";
import { defineCity } from "../citySchema";

export default defineCity({ id: "yopal", name: "Yopal", coordinates: { lat: 5.3378, lng: -72.3959 }, universities });
