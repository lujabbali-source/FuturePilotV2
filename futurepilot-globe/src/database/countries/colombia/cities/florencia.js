import universities from "../universities/florencia";
import { defineCity } from "../citySchema";

export default defineCity({ id: "florencia", name: "Florencia", coordinates: { lat: 1.6144, lng: -75.6062 }, universities });
