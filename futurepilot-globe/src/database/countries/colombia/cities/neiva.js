import universities from "../universities/neiva";
import { defineCity } from "../citySchema";

export default defineCity({ id: "neiva", name: "Neiva", coordinates: { lat: 2.9273, lng: -75.2819 }, universities });
