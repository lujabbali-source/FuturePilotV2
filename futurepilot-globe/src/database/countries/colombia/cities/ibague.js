import universities from "../universities/ibague";
import { defineCity } from "../citySchema";

export default defineCity({ id: "ibague", name: "Ibagué", coordinates: { lat: 4.4389, lng: -75.2322 }, universities });
