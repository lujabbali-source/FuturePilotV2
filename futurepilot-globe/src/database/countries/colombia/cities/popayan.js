import universities from "../universities/popayan";
import { defineCity } from "../citySchema";

export default defineCity({ id: "popayan", name: "Popayán", coordinates: { lat: 2.4448, lng: -76.6147 }, universities });
