import universities from "../universities/pasto";
import { defineCity } from "../citySchema";

export default defineCity({ id: "pasto", name: "Pasto", coordinates: { lat: 1.2136, lng: -77.2811 }, universities });
